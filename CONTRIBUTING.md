# 开发与维护

## 开始开发

Windows 开发环境需要 Node.js/npm。使用锁文件安装依赖，避免开发机自行升级依赖导致构建不一致。

```powershell
npm ci
npm start
```

项目使用 CommonJS、`'use strict'`、两空格缩进和分号。新增用户可见文案同时维护中英文；代码注释解释约束和原因。不要手改 `renderer/tui-editor/` 的生成文件或依赖源码。修改打包入口后使用 `npm run build:renderer` 重新生成资源。

## 模块边界

- `main.js`：窗口生命周期、系统对话框、文件权限、磁盘读写和菜单。
- `preload.js`：明确列出的 IPC 能力；保持上下文隔离，不暴露原始 Node.js 或任意 IPC。
- `renderer/app.js`：文档交互和编辑器状态；共用逻辑放入 `lib/`，界面语言来自 `lib/app-i18n.js`。
- `lib/`：文件监听、恢复备份、偏好、更新检查等可独立验证的逻辑。
- `scripts/windows-release.js`：Windows 品牌名、版本、图标与产物命名；版本来自 `package.json`。

## 文档状态约束

1. 保存只确认发起时的内容快照。保存期间的新输入仍为未保存状态，不能因此关闭窗口或替换文档。
2. 同一窗口同时只有一次文档切换；切换前等待已有保存完成。读取期间的新输入需要重新确认。
3. 读取文档和激活文档分开：只有界面接受新内容后，主进程才切换文件监听和最近打开记录。
4. 备份按窗口分别保存，恢复提示和恢复完成均不消费备份；实际保存或用户明确放弃后才清理。空字符串也可能是需要恢复的修改。
5. 文件监听的异步结果必须属于当前路径和当前监听周期。自身写盘成功后立即等待 `markOwnWrite`，不能用固定时间窗口屏蔽外部修改。
6. 语言和主题切换保留编辑器实例及撤销历史；切换文档才重建编辑器。阅读定位使用实际滚动的 `.ProseMirror`。
7. 偏好和最近文档采用防抖写盘，正常退出时同步完成待写入内容。
8. 文档保存使用同目录临时文件、同步落盘和原子替换；失败不回退为直接截断写入。跟随符号链接的目标；硬链接的其他目录项不会随原子替换一起改变。不承诺硬件断电恢复或保留平台特有扩展属性。

## 验证

```powershell
npm run check                # 自有 JavaScript 语法检查
npm test                     # 开源清单和 Node 单元测试
npm run test:all             # 完整发布检查，任一失败立即停止
```

完整检查按顺序执行语法、开源清单、单元测试及 12 组 Electron 回归：编辑、打印、阅读、搜索、链接、关闭、多窗口、侧栏、帮助、中英文、异步文档操作、恢复与持久化。Electron 测试需要可用的 Windows 桌面会话。临时探针不属于发布检查。

修复异步问题时，用可控延迟复现操作顺序；不能只检查最后的 DOM 文本。涉及恢复和保存时，同时检查编辑器内容、未保存状态和磁盘记录。测试通过不等于已经验证安装包，打包后仍需单独核对资源和实际启动。

## 发布

1. 更新 `package.json`、`package-lock.json` 版本及 `CHANGELOG.md`。依赖升级单独说明原因；更新开源清单遵循 [开源维护规则](docs/open-source.md)。
2. 运行 `npm run test:all`，保留命令与结果。
3. 运行 `npm run dist:installer` 和 `npm run dist:portable`；构建脚本拒绝缺失或空产物。
4. 解包核对版本、代码、图标、`LICENSE` 和两个第三方声明。实际启动便携包，检查中英文切换、内容保留及窗口图标；需要时检查安装/升级流程。
5. 计算 SHA-256；提交到目标分支并核对远端提交。创建版本标签和草稿 Release，上传安装包与便携包。
6. 核对上传文件及校验值后发布为稳定版，再验证更新检查能从旧版识别新版本。

自动回归、包内容验证、实际启动和人工安装测试是不同证据，发布说明应如实区分。不得强推覆盖远端历史或提交用户数据、凭据、临时测试目录。
