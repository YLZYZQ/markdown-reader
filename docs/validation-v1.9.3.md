# 1.9.3 稳定版验证记录

日期：2026-09-22。环境：Windows x64，Node.js 24.13.0，Electron 31.7.7（锁文件版本）。

| 检查 | 本次结果 |
| --- | --- |
| `npm run test:all` | 通过：57 个自有 JavaScript 文件语法检查、开源声明、116 项单元测试、12 组 Electron 回归 |
| 单元测试跳过项 | 1 项真实文件符号链接测试；当前 Windows 用户无创建权限。符号链接解析路径的适配器测试通过 |
| `npm run dist:installer` | 生成 Windows x64 NSIS 安装包 |
| `npm run dist:portable` | 生成 Windows x64 ZIP 便携包 |
| 解包比对 | 安装包 `app.asar` 与便携包的 40 个代码、资源、许可证文件均与工作区逐字节一致；版本均为 1.9.3 |
| 便携包实际运行 | 已保存英文偏好启动正常；中英文来回切换保留同一编辑器和正文；新版图标正常加载 |
| Windows 图标与元数据 | 原生窗口图标和 EXE 图标 32×32 比较差异为 0；产品名为 Markdown阅读器，文件/产品版本为 1.9.3 |

新增回归覆盖保存过程中输入、保存失败、切换等待保存、读取途中再次编辑、取消切换、异步插图、非预设字号、多窗口独立恢复、空内容恢复、恢复接管期间关闭窗口、恢复后直接保存、备份独立清理、旧监听异步结果和失败写盘保留原文。链接回归同时检查定位后的延迟动画不会把标题移出可见区域。

退出持久化测试直接触发实际 `will-quit` 监听器，验证防抖队列立即落盘；这属于事件级验证，不是断电测试。本次未在用户现有安装目录执行覆盖安装，也未宣称完成所有 Windows 版本的兼容性验证。

## 发布文件 SHA-256

```text
696dafa18513d76ec8672fd12168fd145ea5f34ba55616bec87a79c5cd39b611  MarkdownReader-Setup-1.9.3.exe
eef9a7e4a437eb9ebc052c38604b48c256eb2b0383a0eca477274803ced4b262  MarkdownReader-Portable-1.9.3.zip
```

安装版与便携版保留原有中英文界面、奶油白主题、新版图标及 GitHub 更新提示。原子替换文档不维持其他硬链接目录项的联动，不承诺保留平台特有扩展属性。
